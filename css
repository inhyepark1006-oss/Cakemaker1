body {
  font-family: Arial;
  text-align: center;
}

canvas {
  border: 2px solid #ccc;
  margin: 10px;
}

.controls {
  display: flex;
  flex-direction: column;
  gap: 10px;
  width: 220px;
  margin: auto;
}

button {
  cursor: pointer;
}